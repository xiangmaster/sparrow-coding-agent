# Sparrow 桌面端设计

## 1. 产品定位

桌面端不是 CLI 的图形包装器，而是 Sparrow 可靠性机制的可视化入口。用户应当能够
直接看到“模型决定了什么、工具实际做了什么、工作区真实发生了什么、完成门为何接受
或拒绝结束”，同时不需要理解 JSONL 或终端输出。

首个版本是单用户、单工作区、单运行会话的本地桌面软件。它不提供云端账号、远程
执行、多 Agent 编排或内置完整代码编辑器。

## 2. 技术选型

- 界面框架：PySide6、Qt Quick 与 QML；原 Qt Widgets 版本保留为故障兜底。
- 后台执行：`QThread`、`QObject`、Signal/Slot。
- 应用层：不依赖 Qt 的 `AgentSession` 和共享 Runtime。
- 状态来源：Agent 的结构化事件与 `AgentResult`，不解析控制台文本。
- 本地记录：沿用权限为 `0600` 的 JSONL 与中文日志。
- 打包：稳定后使用 Qt 官方 `pyside6-deploy` 生成 macOS `.app`。
- 样式：QML 自定义组件、系统字体和矢量图元；不引入 WebView。

选择 PySide6 的主要原因是现有核心完全使用 Python，能够直接复用数据模型和测试，
也能生成真正的桌面应用。Qt Quick 把状态、过渡、列表和自定义控件作为声明式界面
能力，适合表达 Agent 的连续运行轨迹。当前阶段不选择 Tauri/React，避免增加 Rust
主程序、Python sidecar、WebView IPC、前后端协议和双重打包的风险。

## 3. 分层架构

```text
Qt Quick / QML
  |- Main.qml：启动页、阶段轨迹、证据带和历史档案
  |- DesktopController：向 QML 暴露属性、列表与操作槽
  `- SessionWorker：在 QThread 中调用 AgentSession.run()
          |
          v
AgentSession（纯 Python）
  |- SessionConfig：任务、工作区、模型和预算
  |- 状态机：idle -> running -> completed/failed/cancelled
  |- 协作式取消标记
  `- 事件观察者与运行结果
          |
          v
Shared Runtime
  |- 加载工作区 .env
  |- 构造 Provider 与 AgentSettings
  |- 注册九个本地工具
  `- 组合 GUI、控制台和磁盘记录器
          |
          v
现有 Agent / Context / Evidence / Workspace / Tools
```

Qt 层不得直接调用具体工具、修改证据账本或拼装 Provider 请求。会话层也不得导入
PySide6，确保它能在无图形环境中运行单元测试，并为未来其他入口保留复用能力。

## 4. 主界面

默认窗口建议最小尺寸为 1180×720。启动页只保留任务输入这个主动作；进入运行或
历史回放后，再展开阶段轨迹和证据检查器：

```text
┌──────────────────────────────────────────────────────────────────────┐
│ Sparrow  行动留下轨迹，完成必须有证据      当前项目   设置   状态  │
├──────────────┬───────────────────────────────┬───────────────────────┤
│ 新建任务      │ 理解 — 检查 — 修改 — 验证 — 完成             │
│              ├───────────────────────────────┬───────────────┤
│ 运行档案      │ 行动—观察轨迹                 │ 本地证据      │
│ 任务与时间    │ 模型决策 / 工具结果           │ 真实差异      │
│ 最终状态      │ 重试 / 压缩 / 完成反馈        │ 验证与完成门  │
├──────────────┴───────────────────────────────┴───────────────────────┤
│ 文件变化                 验证状态                  完成门状态      │
└──────────────────────────────────────────────────────────────────────┘
```

### 左栏：导航与历史

- 新建任务入口。
- 当前工作区名称。
- 从 `.sparrow/runs` 读取最近运行。
- 点击历史记录只做离线重放，不重新请求模型或执行工具。
- 历史差异只依据轨迹中已经记录的补丁、替换、重命名和删除参数展示；无法还原完整
  旧内容时明确说明，不读取当前文件冒充历史状态。

### 中栏：行动—观察时间线

- 用“理解—检查—修改—验证—完成”阶段轨迹建立稳定的信息层级。
- 模型响应显示轮次和工具调用数量，推理内容默认折叠。
- 文件工具显示路径与操作类型。
- 命令显示参数、退出码、耗时和输出摘要。
- 失败事件使用明确文字，不只依赖颜色。
- 上下文压缩、Provider 重试和完成门拒绝均有独立事件卡片。
- 任务设置收进弹出面板，不长期占据主界面；停止按钮在运行中保持可用。

### 右栏：证据面板

- 模型、推理强度、迭代上限和上下文预算。
- 初始快照与当前快照之间的真实文件差异。
- 最后一次修改后的验证命令、退出码和摘要。
- 完成门接受或拒绝的原因。
- JSONL 轨迹位置和剩余风险。

底部证据带持续显示文件变化、验证状态和完成门结论。这是 Sparrow 区别于普通聊天式
Agent 的主要视觉语义：运行不是一串对话，而是一条最终必须落到本地证据的行动轨迹。

## 5. 会话状态与取消

合法状态转换如下：

```text
idle -> running -> completed
                -> failed
                -> cancelling -> cancelled
```

同一个 `AgentSession` 只允许运行一次，避免复用旧证据或取消标记。点击停止后立即把
界面状态改为“正在取消”，设置取消标记，并等待当前 Provider 请求或工具调用返回；
不能通过杀线程造成文件只写一半。最终结果必须以 `StopReason.CANCELLED` 记录。

## 6. 安全与隐私

- GUI 只显示 `.env` 是否已配置，永不读取或展示 Key 文本。
- 工作区选择使用系统目录对话框，选择后仍由 `Workspace` 做规范化与越界检查。
- 轨迹可能包含代码和模型观察，历史页明确提示不要随意分享。
- GUI 不提升 `run_command` 权限，也不把受限子进程描述为系统沙箱。
- 删除文件等操作仍必须通过现有工具和真实快照证据，不由界面直接执行。

## 7. 首轮验收标准

- 在没有真实 API 的测试中可以启动会话、接收事件、完成、失败和取消。
- Qt 主线程不执行 Agent 或 Provider 调用。
- 运行中窗口可移动、重绘，停止按钮可响应。
- CLI 全部既有测试保持通过。
- GUI 依赖未安装时，CLI 仍可正常安装和运行，并给出明确安装提示。
- 最小窗口和默认窗口下无控件重叠、截断或不可访问。

QML 视觉装配通过离屏窗口构造、页面切换和资源打包做独立冒烟测试；旧版 Qt Widgets
装配仍明确排除在核心覆盖率之外。`DesktopController`、共享 Runtime、Session、取消、
事件、Agent 和证据逻辑进入常规覆盖率统计，不能用界面截图替代。
