# Sparrow 技术设计

## 1. 设计边界

Sparrow 是单用户、本地运行的编程智能体，提供命令行和 Qt Quick 桌面入口。系统只使用
DeepSeek 的模型 API 与原生工具调用协议；任务上下文、工具定义、本地执行、循环终止、
错误恢复、持久化和完成检查均由仓库内代码实现。

项目不包含多 Agent 编排、远程工作区和供应商托管执行功能。文件工具有明确的工作区边界，
命令工具只提供降低风险的限制，不把本地进程描述成操作系统级沙箱。

## 2. 技术选型

| 领域 | 实现 | 目的 |
| --- | --- | --- |
| 语言与打包 | Python 3.11+、`pyproject.toml`、`src/` 布局 | 控制流透明，安装与测试可复现 |
| 模型客户端 | `openai` Python 包连接 DeepSeek Chat Completions | 只复用传输客户端，不引入 Agent 框架 |
| 桌面界面 | PySide6、Qt Quick、QML | 与 Python 核心直接通信，无额外 Web 服务 |
| 持久化 | JSON 与版本化 JSONL | 会话可恢复，运行轨迹可离线审计 |
| 测试 | pytest 与 ScriptedProvider | 无网络地复现多轮工具调用和失败恢复 |

默认模型、推理强度、单次输出上限和请求超时由 Sparrow 根目录的 `.env` 配置；桌面设置和
CLI 参数可以覆盖运行选项。目标代码仓库中的 `.env` 永远不作为 Provider 配置来源。

## 3. 运行架构

```text
Qt Quick / CLI
      │
      ├─ DesktopController：将结构化事件投影为界面状态
      └─ ConversationSession：保存多轮消息、任务设置与运行状态
                    │
                  Agent：迭代、预算、重复行为检测和终止控制
             ┌──────┼──────────┐
       ModelProvider  ToolRegistry  CompletionGate
             │            │              │
      DeepSeekProvider  Workspace     EvidenceLedger
             │            │              │
          SSE 响应     本地工具执行    快照与验证时序
```

桌面端在工作线程中运行会话，通过 Qt Signal/Slot 把不可变的 `SessionEvent` 送回主线程。
QML 只绑定控制器公开的属性、列表和操作槽，不直接执行工具或修改证据。CLI 直接装配同一套
Provider、工具注册表、记录器和 Agent，因此两个入口具有一致的执行语义。

## 4. Agent 循环

1. 从当前会话恢复上下文，并追加新的用户消息。
2. 将系统规则、消息历史和工具 JSON Schema 发送给 Provider。
3. Provider 以 SSE 接收增量文本，同时拼装完整响应和工具调用参数。
4. Agent 校验工具名称与参数；非法调用被转换成结构化观察结果，而不是令进程崩溃。
5. 合法工具在本地执行，结果和工作区变化回填上下文并写入运行轨迹。
6. 模型通过 `request_completion` 提交摘要、修改文件、验证命令和剩余风险。
7. 完成证据门核对本地快照与验证时序；通过则结束，否则反馈缺失证据并继续循环。

最大迭代次数、累计 Token 预算、用户取消、重复调用熔断和不可恢复的 Provider 错误都能
触发明确终止。预算或迭代耗尽时，最终回复会保留已确认的修改、最近验证和继续方式。

## 5. 本地工具

Sparrow 向模型提供十个工作区工具，并由 Agent 额外提供结构化完成申请：

| 工具 | 作用 |
| --- | --- |
| `list_files` | 列出过滤后的工作区文件树 |
| `read_file` | 按行读取受大小限制的 UTF-8 文件 |
| `search_files` | 搜索文本并返回路径、行号和内容 |
| `create_directory` | 在边界内创建目录 |
| `create_file` | 创建新文件且拒绝覆盖已有目标 |
| `replace_text` | 按预期匹配次数执行精确替换 |
| `apply_patch` | 校验并应用统一差异补丁 |
| `rename_file` | 在不覆盖目标的前提下重命名普通文件 |
| `delete_file` | 删除普通文件并由快照复核结果 |
| `run_command` | 执行受时间、输出和命令策略限制的本地进程 |

所有文件路径先经过规范化和符号链接检查。工具拒绝工作区外路径、版本控制内部目录、
`.env` 与常见凭据文件，并限制输入、输出和文件大小。工具失败作为数据返回模型，使 Agent
能够调整方案；只有明确的控制器终止条件才结束循环。

## 6. 上下文与多轮会话

`ConversationSession` 在同一任务中复用消息上下文，每条用户消息形成独立 Turn，并分别
经过完成证据门。会话保存到 `.sparrow/threads/`，包含模型设置、消息、终止原因、Token
用量和轨迹引用；写入采用临时文件替换，目录与文件权限受到限制。

当上下文超过字符预算时，Sparrow 以完整助手轮次为单位压缩较早历史，保留系统规则、
原始任务、确定性事实摘要和最近的工具因果链。Assistant 工具调用及其 Tool 结果不会被拆开，
DeepSeek 协议需要的 `reasoning_content` 也会保留在协议状态中。

## 7. 变更与完成证据

Agent 启动时为可操作文件建立包含内容和权限的 SHA-256 快照，并在工具调用后刷新。
依赖目录、缓存、Git 内部目录、Sparrow 轨迹和凭据不进入快照。因而即使项目命令间接修改
文件，系统仍能识别真实变化。

完成申请必须满足以下条件：

- 模型声明的修改文件与初始、当前快照差异一致；
- 最后一次真实变化之后运行过相关验证；
- 验证命令退出码为零，且没有未解决的工具错误；
- 纯解释任务没有伪造文件修改或验证记录。

成功命令如果自身改变了文件，不能同时证明变化后的状态，必须再次执行验证。完成门只证明
“存在可检查证据”，不把测试通过夸大为程序绝对正确，最终判断仍由用户完成。

## 8. 差异审查与审计

修改工具执行前后都会捕获真实文件内容并生成统一差异。桌面端按文件聚合修改，使用带行号的
红删绿增视图展示；文件标签支持横向滚动和悬停预览。旧版单次 JSONL 轨迹仍可离线读取，
但恢复任务以持久化 Thread/Turn/Message 为准。

每次记录运行会在 `.sparrow/runs/` 写入版本化 JSONL 和中文摘要日志，包含模型响应、工具调用、
结果、验证和终止原因。离线重放只读取轨迹，不请求模型，也不重新执行任何工具。

## 9. 错误处理与测试

Provider 将网络、协议和请求错误分类；可恢复错误使用有上限的退避重试。重复行为检测先向
模型反馈，再在超过阈值后熔断。取消采用协作式检查点语义：已开始的请求或进程安全返回后，
Agent 不再启动下一步。

测试覆盖路径边界、敏感文件、工具 Schema、文件变更、命令限制、上下文压缩、流式 Provider、
多轮持久化、历史恢复、完成证据、QML 控制器和离屏界面装配。除显式标记的真实 API 冒烟测试外，
测试均使用本地临时工作区和 ScriptedProvider，不访问模型服务。
