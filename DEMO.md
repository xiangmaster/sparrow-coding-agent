# 固定演示指南

本演示用一个小型但完整的校园快递配送费项目，展示 Sparrow 的核心链路：
检查真实项目、执行失败测试、定位边界缺陷、应用补丁、重新验证、通过完成证据门，
最后对整个过程进行无副作用重放。演示模板始终保留缺陷，每次录制前都能一键重建，
避免上一次运行结果影响下一次演示。

## 一、准备一次干净演示

先在仓库根目录完成项目安装，并确认 `.env` 已填写真实的
`DEEPSEEK_API_KEY`。随后执行：

```bash
.venv/bin/python scripts/prepare_demo.py
```

脚本会把模板复制到被 Git 忽略的 `.sparrow/demo-workspace/`，并把根目录
`.env` 安全复制到演示工作区。它不会打印密钥，目标 `.env` 权限为 `0600`。
再次执行同一命令即可丢弃上次修改并恢复初始缺陷。

## 二、展示初始失败

```bash
cd .sparrow/demo-workspace
python3 -m unittest -v
cd ../..
```

测试应报告金额门槛和重量进位相关失败。这一步说明后续任务不是预先做好的假演示。

## 三、让 Sparrow 完成任务

```bash
.venv/bin/sparrow run \
  "阅读需求并修复配送费计算缺陷，使全部测试通过；不要修改测试文件。" \
  --workspace .sparrow/demo-workspace \
  --model deepseek-v4-flash \
  --reasoning-effort low \
  --max-iterations 16
```

观察终端中的模型轮次和工具调用。理想流程包含读取文件、运行测试、修改
`delivery.py`、再次运行测试，以及 `request_completion` 通过证据检查。
命令返回码为 `0` 且显示 `终止原因：completed` 才算演示成功。

由于真实模型输出存在随机性，正式录屏前至少预演两次。如果一次运行未完成，先执行
准备命令恢复现场，再重试；不要直接在已经被修改的工作区上重复录制。

## 四、重放审计轨迹

```bash
.venv/bin/python scripts/replay_latest_demo.py
```

重放只读取 JSONL，不请求 DeepSeek，也不会再次修改文件或执行测试。这是 Sparrow
区别于普通“黑盒跑完即结束”演示的自然收尾：老师能看到终止原因、修改文件、验证记录
和事件顺序，同时不会让审计动作产生新的副作用。

## 两分钟录屏节奏

- `0:00-0:15`：一句话说明任务，运行初始测试展示真实失败。
- `0:15-1:20`：运行 Sparrow；可加速等待模型的片段，但保留工具名和关键失败/成功结果。
- `1:20-1:40`：展示 `completed`、修改文件和退出码为 0 的验证记录。
- `1:40-2:00`：重放轨迹，并说明“模型提出完成，本地证据门决定是否接受”。

录屏时不要打开 `.env`，不要展示包含密钥的窗口，也不要把 `.sparrow/` 加入 Git。
